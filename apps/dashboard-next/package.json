{
  "name": "webalytics-dashboard-next",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal read-only dashboard for the webalytics service. Renders /v1/stats/* data via server components so the bearer token never leaves the server.",
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001"
  },
  "dependencies": {
    "@jlaviole90/dashboard-react": "*",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.4"
  }
}
